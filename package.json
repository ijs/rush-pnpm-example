{
  "name": "root",
  "private": true,
  "engines": {
    "node": ">=14.15.0 <15.0.0"
  },
  "scripts": {
    "preinstall": "node common/scripts/install-run-rush.js install",
    "build": "rm -fr docs && node common/scripts/install-run-rush.js build -p 3 -i vueapp -v",
    "rebuild": "node common/scripts/install-run-rush.js rebuild -p 3 -i vueapp -v",
    "local:publish": "rush change && rush publish -a -p"
  }
}
